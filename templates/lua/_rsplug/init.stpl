-- Auto generated by rsplug
local pkgid2scripts = {<% for (id, script_set) in pkgid2scripts {%>['<%=id%>']={<% for (script_type, module_id) in script_set {%>['<%=script_type%>']='<%=module_id%>',<%}%>},<%}%>}
return {
	---@param pkg string
	packadd = function(pkg)
		local setup_scripts = pkgid2scripts[pkg] or {};
		vim.cmd.packadd(pkg)
		if setup_scripts.lua_source then
			require(setup_scripts.lua_source)
		end
	end,
}
