-- Auto generated by rsplug
for _, cmd in ipairs {<% for val in events {%>'<%= val %>',<%}%>} do
	vim.api.nvim_create_autocmd(cmd, {callback=function(...) require '_rsplug/on_event'.autocmd_callback(...) end, once=true})
end
