-- Auto generated by rsplug
<% for (event, ids) in event2pkgid { %>
vim.api.nvim_create_autocmd('<%= event %>', {callback=function(ctx)
	require '_rsplug'.autocmd_callback(ctx,{<% for id in ids {%>'<%= id %>',<%}%>})
end, once=true})
<%}%>
