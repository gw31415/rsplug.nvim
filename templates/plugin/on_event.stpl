-- Auto generated by rsplug
for _, ev in ipairs {<% for ev in events {%>'<%= ev %>',<%}%>} do
	vim.api.nvim_create_autocmd(ev, {callback=function(...) require '_rsplug/on_event'.autocmd_callback(...) end, once=true})
end
