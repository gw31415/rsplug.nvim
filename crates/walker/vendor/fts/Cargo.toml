# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
name = "rsplug-fts"
edition = "2015"
version = "0.4.0"
authors = ["dalance <dalance@gmail.com>"]
exclude = ["test_data/*", ".cargo_vcs_info.json", ".cargo-ok", "Cargo.toml.orig"]
description = "Vendored fork of dalance/fts-rs for rsplug.nvim"
readme = "README.md"
keywords = ["libc", "directory", "walk", "recursive", "iterator"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/gw31415/rsplug.nvim"
# The upstream bench (benches/fts.rs) requires the nightly-only `#![feature(test)]`.
# rsplug.nvim pins a stable toolchain and does not run these benches, so disable
# auto-discovery to keep `--all-target` builds (e.g. `cargo clippy --all-targets`)
# green on stable.
autobenches = false

[lib]
name = "fts"
[package.metadata.release]
dev-version-ext = "pre"
pre-release-commit-message = "Prepare to v{{version}}"
pro-release-commit-message = "Start next development iteration v{{version}}"
tag-message = "Bump version to {{version}}"
tag-prefix = "v"

[[package.metadata.release.pre-release-replacements]]
file = "README.md"
replace = "fts = \"{{version}}\""
search = "fts = \"[a-z0-9\\.-]+\""
[dependencies.bitflags]
version = "2"

[dependencies.enum_primitive]
version = "0.1"

[dependencies.libc]
version = "0.2"

[dependencies.num]
version = "0.4"

[dev-dependencies.walkdir]
version = "2"

[badges.codecov]
branch = "master"
repository = "dalance/fts-rs"
service = "github"

[badges.travis-ci]
repository = "dalance/fts-rs"